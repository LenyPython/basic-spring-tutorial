
spring:
    datasource:
        url: jdbc:postresql://localhost:5432/SpringData
        username: admin
        password: admin
    jpa:
        hibernate:
            ddl-auto: create-drop
        show-sql: true
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
                format_sql: true